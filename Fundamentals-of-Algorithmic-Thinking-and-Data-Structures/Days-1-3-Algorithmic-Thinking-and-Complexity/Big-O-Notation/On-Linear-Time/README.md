# O(n) Linear Time

An algorithm is `O(n)` when its work grows in direct proportion to the input size.

## Example

Traversing an array once to print all elements is linear time.

## Intuition

If the input doubles, the work also roughly doubles.
