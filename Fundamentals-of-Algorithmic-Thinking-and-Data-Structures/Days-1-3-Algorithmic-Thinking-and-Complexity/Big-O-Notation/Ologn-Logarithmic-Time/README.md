# O(log n) Logarithmic Time

An algorithm is `O(log n)` when it repeatedly cuts the problem size down.

## Example

Binary search is the standard logarithmic-time example.

## Intuition

Each step removes about half of the remaining search space, so growth stays slow even for large inputs.
