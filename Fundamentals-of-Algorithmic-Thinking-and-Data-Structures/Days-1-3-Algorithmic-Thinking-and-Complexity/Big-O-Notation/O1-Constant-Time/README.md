# O(1) Constant Time

An algorithm is `O(1)` when the number of steps does not depend on the input size.

## Example

Accessing `arr[5]` in an array takes constant time.

## Intuition

Whether the array has 10 elements or 10,000 elements, a direct index lookup still takes the same kind of work.
