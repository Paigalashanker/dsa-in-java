# O(n^2) Quadratic Time

An algorithm is `O(n^2)` when it usually uses two nested loops over the same input.

## Example

Comparing every pair of elements in an array is commonly quadratic time.

## Intuition

If the input size doubles, the work grows by about four times.
